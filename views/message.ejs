<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Message</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>Message from <%= message.user %></h1>

    <div class="message">
      <p class="user"><%= message.user %></p>
      <p><%= message.text %></p>
      <p class="date"><%= new Date(message.added).toLocaleString() %></p>
    </div>

    <a class="new-message" href="/">Back to Messages</a>
  </body>
</html>
